# Declarado en la adopción (2026-08-26).
#   harness     = este repo ES el harness
#   application = producto con app (este caso)
#   other       = doc-only / otros
# Formato: `clave: valor`. Este archivo es TUYO: el sync jamás lo pisa.
project_kind: application
