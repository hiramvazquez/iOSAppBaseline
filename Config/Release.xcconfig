// Configuración de Release.
#include? "Secrets.xcconfig"

SWIFT_COMPILATION_MODE = wholemodule
VALIDATE_PRODUCT = YES
// Solo el HOST: el esquema lo impone el codigo (https, no configurable) y el
// prefijo /3 lo traen ya los `path` de cada request. Ver TMDBConfiguration.
// Vive aqui y no en Secrets.xcconfig para que un clon recien hecho tenga host
// por defecto y solo le falte el token.
TMDB_HOST = api.themoviedb.org
